server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/chaitanyajc05/spring-cloud
          search-paths:
          - app*
          force-pull: true
          